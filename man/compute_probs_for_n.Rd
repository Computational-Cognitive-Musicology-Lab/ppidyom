% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PPM_R.R
\name{compute_probs_for_n}
\alias{compute_probs_for_n}
\title{Compute PPM probabilities for a given order}
\usage{
compute_probs_for_n(
  countMatrix,
  N,
  escape_method = escape_C,
  prob_count_type = "Ce"
)
}
\arguments{
\item{countMatrix}{A \code{data.table} with lagged counts from \code{dynamicModel}.}

\item{N}{Integer. The order to compute probabilities for.}

\item{escape_method}{Function. An escape function that returns \code{denom}, \code{esc}, and \code{subtract}.}

\item{prob_count_type}{Character. Determines which counts are used to compute probabilities:
\describe{
  \item{"C"}{Divident for probability of an event is the number of times this context has occurred, consistent with Harrison (2020)'s PPM.}
  \item{"Ce"}{Divident for probability of an event is the number of times this event has occurred after this context, consistent with Pearce (2005)'s paper.}
}}
}
\value{
A \code{data.table} identical to \code{countMatrix} with added columns:
  \code{prob}, \code{esc}, \code{C}, \code{t}, \code{t1}, \code{event_count}.
}
\description{
Computes the probability of each event given its context at a specific order,
using a specified escape method and count type.
}
\examples{
x <- c("A", "B", "A", "C")
cm <- dynamicModel(x, N = 2)
compute_probs_for_n(cm, N = 2, escape_method = escape_C, prob_count_type = "Ce")
}
