% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/userfunctions.R
\name{run_ppidyom}
\alias{run_ppidyom}
\title{Run PPM over a corpus of sequences}
\usage{
run_ppidyom(
  seq_list,
  N,
  alphabet = NULL,
  model_type = c("stm", "ltm", "both", "ltm+", "both+"),
  ppm_type = c("interpolation", "backoff"),
  stm_lambda = "C",
  ltm_lambda = "C",
  stm_exclusion = TRUE,
  ltm_exclusion = TRUE,
  stm_update_exclusion = TRUE,
  ltm_update_exclusion = FALSE,
  b = 1,
  idyom_base = FALSE,
  ltm_start_token = TRUE
)
}
\arguments{
\item{seq_list}{List of character-vector sequences (the corpus). Each
element is one sequence, e.g. one piece.}

\item{N}{Maximum n-gram order (order bound).}

\item{alphabet}{Character vector of all possible symbols. If \code{NULL}
(default), inferred as the set of unique symbols across \code{seq_list}.}

\item{model_type}{Which memory component(s) to use:
\itemize{
\item \code{"stm"} — short-term memory, one sequence at a time, no leave-one-out.
\item \code{"ltm"} — long-term memory, pretrained on the corpus, no update during test.
\item \code{"both"} — STM + LTM blended.
\item \code{"ltm+"}/\code{"both+"} — as \code{"ltm"}/\code{"both"}, but LTM updates online per event.
}}

\item{ppm_type}{PPM estimation method:
\itemize{
\item \code{"interpolation"} — weighted sum across all n-gram orders.
\item \code{"backoff"} — falls through orders from longest to shortest matching context.
}}

\item{stm_lambda}{Escape/discount method for STM: \code{"A"}, \code{"B"}, \code{"C"}
(Witten-Bell, default), \code{"D"} (absolute discounting), or \code{"X"} (AX, based
on singleton counts). See the
\href{../articles/ParameterCorrespondence.html#escape-method}{Escape method}
section of the \href{../articles/ParameterCorrespondence.html}{Parameter Correspondence}
vignette for the exact formulas.}

\item{ltm_lambda}{Escape/discount method for LTM; same options as \code{stm_lambda}.}

\item{stm_exclusion}{Logical; exclude symbols already assigned a probability
at a higher order when computing STM escape probabilities (default TRUE).}

\item{ltm_exclusion}{Logical; same as \code{stm_exclusion}, for LTM (default TRUE).}

\item{stm_update_exclusion}{Logical; stop updating lower-order STM counts
once a higher order already matched at this timestep (default TRUE).}

\item{ltm_update_exclusion}{Logical; same as \code{stm_update_exclusion}, but
applied while accumulating LTM counts (default FALSE).}

\item{b}{Bias exponent for entropy-weighted blending, used only when
\code{model_type} is \code{"both"}/\code{"both+"}; higher values favor whichever of
STM/LTM is currently more confident (default 1).}

\item{idyom_base}{Logical; use IDyOM's order-(-1) base distribution instead
of the default shrinking-denominator base (default FALSE). See the
\href{../articles/ImplementationDiscrepancy.html}{Implementation Discrepancy} vignette.}

\item{ltm_start_token}{Logical; count beginning-of-sequence positions when
accumulating LTM (default TRUE; set FALSE to match IDyOM).}
}
\value{
A list the same length as \code{seq_list}; each element is a data.table
with columns \code{index}, \code{Event}, \code{P}, \code{IC}, \code{Entropy}, \code{seq_id} (the position
of that sequence in \code{seq_list}). Combine with \code{data.table::rbindlist()}.
}
\description{
Lower-level building block behind \code{\link[=ppidyom]{ppidyom()}}. Takes a plain list of
sequences (rather than viewpoint vectors + grouping columns) and, for
\code{model_type}s with an LTM component, evaluates each sequence leave-one-out:
the whole corpus is trained first, then for each sequence in turn it is
detrained, predicted on, and retrained before moving to the next.
}
\examples{
# run_ppidyom is internal; use ::: since this example isn't run via library()
seq1 <- c("A", "B", "A", "C", "A", "B", "A", "C", "A")
seq2 <- c("A", "B", "C", "A", "B", "C", "A", "B", "C")
seq3 <- c("B", "A", "B", "C", "A")

# STM only, no leave-one-out (each sequence starts from scratch):
res_stm <- ppidyom:::run_ppidyom(
  seq_list = list(seq1, seq2, seq3), N = 3, model_type = "stm",
  stm_exclusion = FALSE, stm_update_exclusion = FALSE
)
data.table::rbindlist(res_stm)

# STM + LTM with leave-one-out:
res_both <- ppidyom:::run_ppidyom(
  seq_list = list(seq1, seq2, seq3), N = 3, model_type = "both"
)
data.table::rbindlist(res_both)
}
