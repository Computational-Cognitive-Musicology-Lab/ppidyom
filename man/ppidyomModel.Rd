% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppidyom.R
\docType{class}
\name{ppidyomModel}
\alias{ppidyomModel}
\title{Initialize a new PPM counter}
\arguments{
\item{N}{Maximum n-gram order (order bound).}

\item{alphabet}{Character vector of all possible symbols/events.}

\item{stm_exclusion}{Logical; when computing STM escape probabilities,
exclude symbols already assigned a probability at a higher order
(default TRUE).}

\item{ltm_exclusion}{Logical; same as \code{stm_exclusion}, for LTM (default TRUE).}

\item{stm_update_exclusion}{Logical; stop updating lower-order STM counts
once a higher order already matched at this timestep (default TRUE).}

\item{ltm_update_exclusion}{Logical; same as \code{stm_update_exclusion}, but
applied while accumulating LTM counts (default FALSE).}

\item{ltm_start_token}{Logical; count beginning-of-sequence positions
when accumulating LTM (default TRUE; set FALSE to match IDyOM).}

\item{x}{Character vector sequence}

\item{model_type}{Which memory component(s) to use:
\itemize{
\item \code{"stm"} — short-term memory, \code{x} only.
\item \code{"ltm"} — long-term memory, from prior \code{train_sequence()} calls.
\item \code{"both"} — STM + LTM blended.
\item \code{"ltm+"}/\code{"both+"} — as \code{"ltm"}/\code{"both"}, but LTM is updated online
after each event of \code{x}.
}}

\item{ppm_type}{PPM estimation method:
\itemize{
\item \code{"interpolation"} — weighted sum across all n-gram orders.
\item \code{"backoff"} — falls through orders from longest to shortest matching context.
}}

\item{stm_lambda}{Escape/discount method for STM:
\itemize{
\item \code{"A"} — very conservative; escapes rarely.
\item \code{"B"} — escapes in proportion to novelty.
\item \code{"C"} — Witten-Bell (default); balances novelty and count stability.
\item \code{"D"} — absolute discounting (d = 0.5).
\item \code{"X"} — AX; escapes based on singleton count.
}

See the \href{../articles/ParameterCorrespondence.html#escape-method}{Escape method}
section of the \href{../articles/ParameterCorrespondence.html}{Parameter Correspondence}
vignette for the exact formulas.}

\item{ltm_lambda}{Escape/discount method for LTM; same options as \code{stm_lambda}.}

\item{b}{Bias exponent for entropy-weighted blending, used only when
\code{model_type} is \code{"both"}/\code{"both+"}; higher values favor whichever of
STM/LTM is currently more confident (lower entropy).}

\item{idyom_base}{Logical order-(-1) base distribution:
\itemize{
\item \code{TRUE} — IDyOM-compatible: 1/|alphabet| when exclusion=FALSE,
shrinking denominator when exclusion=TRUE.
\item \code{FALSE} (default) — always the shrinking denominator (matches
Harrison's ppm package).
}

See the \href{../articles/ImplementationDiscrepancy.html}{Implementation Discrepancy}
vignette for details.
(Matching IDyOM for LTM also requires the constructor's \code{ltm_start_token = FALSE}.)}
}
\value{
data.table with columns: index, Event, P, IC, Entropy
}
\description{
Initialize a new PPM counter
}

\examples{
# ppidyomModel is internal; use ::: since this example isn't run via library()
model <- ppidyom:::ppidyomModel$new(N = 3, alphabet = c("A", "B", "C"), stm_exclusion = TRUE)
result <- model$predict_sequence(
  c("A", "B", "A", "C", "A", "B", "A", "C", "A"),
  model_type = "stm", stm_lambda = "C"
)
result[, .(index, Event, P, IC, Entropy)]
}
